<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tomasz.studiesmenager">

    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <meta-data
            android:name="DATABASE"
            android:value="manager.db" />
        <meta-data
            android:name="VERSION"
            android:value="5" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="com.example.tomasz.studiesmenager" />

        <activity android:name=".Main"></activity>
        <activity android:name=".DismissActivity" />
        <receiver android:name=".NotificationReceiver">
            <intent-filter>
                <action android:name="YES_ACTION"/>
                <action android:name="STOP_ACTION"/>
                <action android:name="GRADE_ACTION"/>
            </intent-filter>
        </receiver>
        <receiver
            android:name=".PushReceiver"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.media.action.DISPLAY_NOTIFICATION"
                    android:theme="@android:style/Theme.NoDisplay"/>
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".SubjectDetail.SubjectDetailActivity"
            android:label="@string/title_activity_subject_detail"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".SubjectsListCardView.SubjectsActivity"
            android:label="@string/title_activity_subjects"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".addView.addViewActivity" />
        <activity android:name=".ScheduleViewActivity"></activity>
    </application>

</manifest>